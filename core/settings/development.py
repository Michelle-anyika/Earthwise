from .base import *

DEBUG = True

# For development, we might want to allow all hosts
ALLOWED_HOSTS = ['*']

# You can add development-specific apps or middleware here
# INSTALLED_APPS += ['debug_toolbar']
# MIDDLEWARE += ['debug_toolbar.middleware.DebugToolbarMiddleware']
